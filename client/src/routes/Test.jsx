import { Ticker } from "./../game/util/Ticker";

// const loop = new Ticker({
// 	fps: 5,
// 	tick: ({ dt, now }) => console.log(dt),
// });

// loop.start();

export function Test() {
	return (
		<div>
		</div>
	);
};

export default Test;