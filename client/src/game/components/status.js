export function status({ state = "normal" } = {}) {
	const compObj = {
		/**
		 * The wayfinding algorithms to use when finding a path
		 */
		state: state,
	};

	return compObj;
};

export default status;