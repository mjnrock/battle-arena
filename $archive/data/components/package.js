import Position, { Name as PositionName }  from "./Position";
import Registrar, { Name as RegistrarName }  from "./Registrar";

export const Components = {
	[ PositionName ]: Position,
	[ RegistrarName ]: Registrar,
};

export default Components;